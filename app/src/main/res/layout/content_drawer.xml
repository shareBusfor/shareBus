<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.activity.DrawerActivity"
    tools:showIn="@layout/app_bar_drawer">

    <RelativeLayout
        android:id="@+id/rl_choose"
        android:layout_width="200dp"
        android:layout_height="40dp"

        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/shape_rec_blue_border">

        <TextView
            android:id="@+id/tv_choose1"
            android:layout_width="110dp"
            android:layout_height="40dp"
            android:text="@string/home_choose_passenger"
            android:background="@drawable/shape_rec_blue"
            android:gravity="center"/>

        <TextView
            android:id="@+id/tv_choose2"
            android:layout_width="110dp"
            android:layout_height="40dp"
            android:layout_marginLeft="90dp"
            android:text="@string/home_choose_carowner"
            android:gravity="center"/>

    </RelativeLayout>
    <ListView
        android:layout_marginTop="@dimen/y30"
        android:layout_below="@+id/rl_choose"
        android:id="@+id/mainListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false"

        android:divider="@android:color/transparent"
        android:dividerHeight="15dp"
        android:paddingBottom="20dp"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="20dp"
        android:scrollbars="none"
      />



</RelativeLayout>